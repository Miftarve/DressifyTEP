<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Noleggio e Vendita</title>
    <style>
        /* Global styles */
body {
    font-family: 'Arial', sans-serif;
    background-color: #f4f7fb;
    color: #333;
    margin: 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
}

h1, h2 {
    color: #2c3e50;
    text-align: center;
}

h1 {
    margin-top: 40px;
    font-size: 2.5em;
}

h2 {
    margin-top: 20px;
    font-size: 1.8em;
}

/* Filter container styles */
.filter-container {
    background-color: #ffffff;
    border: 1px solid #ddd;
    border-radius: 8px;
    padding: 20px;
    width: 250px;  /* Ridotta la larghezza della card dei filtri */
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    margin: 30px 0;
    align-self: flex-start;  /* Allineato a sinistra */
}

.filter-container label {
    font-size: 1.1em;
    color: #34495e;
    margin-bottom: 5px;
    display: block;
}

.filter-container input {
    padding: 10px;
    width: 100%;
    margin-bottom: 15px;
    border: 1px solid #ddd;
    border-radius: 6px;
    font-size: 1em;
    color: #333;
}

.filter-container button {
    padding: 10px 20px;
    background-color: #3498db;
    color: white;
    border: none;
    border-radius: 6px;
    font-size: 1.1em;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

.filter-container button:hover {
    background-color: #2980b9;
}

/* Card styles for products */
#products {
    display: grid;
    grid-template-columns: repeat(3, 1fr);  /* Tre card per riga */
    gap: 20px;
    width: 80%;
    max-width: 1100px;
    margin: 20px 0;
    justify-content: flex-start; /* Allineamento a sinistra */
}

.card {
    background-color: #ffffff;
    border: 1px solid #ddd;
    border-radius: 10px;  /* Bordo arrotondato più morbido */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    max-width: 280px;  /* Limita la larghezza massima delle card */
    width: 100%;
    margin: 0 auto;
}

.card:hover {
    transform: translateY(-5px);
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
}

.card h3 {
    font-size: 1.4em;
    color: #2c3e50;
    margin-bottom: 10px;
    text-transform: capitalize; /* Aggiunto per stilizzare il nome */
}

.card p {
    font-size: 1.1em;
    color: #7f8c8d;
    margin: 5px 0;
}

/* Button styles inside cards */
.card button {
    padding: 10px 20px;
    background-color: #27ae60;
    color: white;
    border: none;
    border-radius: 6px;
    font-size: 1.1em;
    cursor: pointer;
    margin-top: 15px;
    transition: background-color 0.3s ease;
    width: 100%; /* Fai i pulsanti della card più grandi */
}

.card button:hover {
    background-color: #2ecc71;
}

.card form {
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
}

/* Responsive styles */
@media (max-width: 768px) {
    .filter-container {
        width: 90%;
    }

    #products {
        grid-template-columns: repeat(2, 1fr);  /* Due card per riga su schermi più piccoli */
    }

    .card {
        width: 100%;
        margin: 10px 0;
    }

    h1 {
        font-size: 2em;
    }

    h2 {
        font-size: 1.5em;
    }
}

    </style>
</head>

<body>

    <h1>Catalogo Noleggio</h1>

    <!-- Form per i filtri -->
    <div class="filter-container">
        <form action="/noleggio" method="get">
            <label for="brand">Brand:</label>
            <input type="text" name="brand" placeholder="Es. Nike" value="{{brand}}">

            <label for="colore">Colore:</label>
            <input type="text" name="colore" placeholder="Es. Rosso" value="{{colore}}">

            <label for="condizione">Condizione:</label>
            <input type="text" name="condizione" placeholder="Es. Nuovo" value="{{condizione}}">

            <label for="prezzoMin">Prezzo Minimo:</label>
            <input type="number" name="prezzoMin" value="{{prezzoMin}}">

            <label for="prezzoMax">Prezzo Massimo:</label>
            <input type="number" name="prezzoMax" value="{{prezzoMax}}">


            <button type="submit">Filtra</button>
        </form>
    </div>

    <h2>Risultati:</h2>

    <!-- Visualizza i prodotti filtrati -->
    {{#if products.length}}
    <div id="products">
        {{#each products}}
        <div class="card">
            <h3>{{brand}} - {{category}}</h3>
            <p>Prezzo: {{price}}€</p>
            <p>Condizione: {{condition}}</p>
            <p>Colore: {{color}}</p>

            <!-- Form per il noleggio -->
            <form action="/noleggio" method="post">
                <input type="hidden" name="id" value="{{id}}">
                <label for="durata">Durata (giorni):</label>
                <input type="number" name="durata" min="1" required>
                <button type="submit">Noleggia</button>
            </form>

            <!-- Form per l'acquisto -->
            <form action="/acquista" method="post">
                <input type="hidden" name="id" value="{{id}}">
                <button type="submit">Acquista</button>
            </form>
        </div>
        {{/each}}
    </div>
    {{else}}
    <p>Nessun prodotto trovato con i filtri applicati.</p>
    {{/if}}

</body>

</html>