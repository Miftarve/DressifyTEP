<!DOCTYPE html>
<html lang="it">

<head>
  <title>Dressmify - Home</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <style>
    /* General Reset and Fonts */
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
    
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    :root {
      --primary: #6a4feb;
      --primary-dark: #5335e8;
      --primary-light: #eceaff;
      --secondary: #ff6b8b;
      --accent: #33d9b2;
      --dark: #2c3e50;
      --gray-dark: #4a5568;
      --gray: #a0aec0;
      --gray-light: #f7fafc;
      --white: #ffffff;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background-color: #f8f9ff;
      color: var(--dark);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding-top: 80px;
      background-image: linear-gradient(135deg, #f8f9ff 0%, #f1f1ff 100%);
      position: relative;
    }

    /* Decorative Icons */
    .decorative-icon {
      position: fixed;
      opacity: 0.08;
      z-index: -1;
    }

    .icon-left {
      left: 5%;
      top: 40%;
      font-size: 20rem;
      color: var(--primary);
      transform: rotate(-15deg);
    }

    .icon-right {
      right: 5%;
      top: 30%;
      font-size: 18rem;
      color: var(--secondary);
      transform: rotate(15deg);
    }

    /* Navbar Styling */
    .navbar {
      width: 100%;
      background-color: var(--white);
      padding: 18px 0;
      box-shadow: 0 4px 20px rgba(106, 79, 235, 0.08);
      position: fixed;
      top: 0;
      z-index: 1000;
      display: flex;
      justify-content: center;
    }

    .navbar-container {
      width: 90%;
      max-width: 1200px;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    .navbar-brand {
      font-size: 1.7rem;
      font-weight: 700;
      color: var(--primary);
      text-decoration: none;
      padding-right: 20px;
      letter-spacing: -0.5px;
      display: flex;
      align-items: center;
    }
    
    .navbar-brand::before {
      content: '';
      display: inline-block;
      width: 12px;
      height: 12px;
      background-color: var(--secondary);
      border-radius: 50%;
      margin-right: 8px;
    }

    .navbar-links {
      display: flex;
    }

    .navbar a {
      color: var(--gray-dark);
      text-decoration: none;
      font-weight: 500;
      padding: 10px 20px;
      margin: 0 8px;
      border-radius: 8px;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
    }

    .navbar a i {
      margin-right: 8px;
      font-size: 1.1rem;
    }

    .navbar a:hover {
      background-color: var(--primary-light);
      color: var(--primary);
      transform: translateY(-2px);
    }

    .navbar a.active {
      background-color: var(--primary);
      color: var(--white);
    }

    /* Main Content */
    main {
      background-color: var(--white);
      border-radius: 16px;
      box-shadow: 0 8px 30px rgba(106, 79, 235, 0.12);
      padding: 50px;
      max-width: 900px;
      width: 90%;
      margin: 60px auto;
      text-align: center;
      animation: fadeIn 0.8s ease-out;
      position: relative;
      overflow: hidden;
    }
    
    main::before {
      content: '';
      position: absolute;
      width: 200px;
      height: 200px;
      background: linear-gradient(135deg, var(--primary-light), rgba(255,255,255,0));
      border-radius: 50%;
      top: -100px;
      right: -100px;
      opacity: 0.4;
    }
    
    main::after {
      content: '';
      position: absolute;
      width: 150px;
      height: 150px;
      background: linear-gradient(135deg, var(--secondary), rgba(255,255,255,0));
      border-radius: 50%;
      bottom: -75px;
      left: -75px;
      opacity: 0.2;
    }

    /* Welcome Section */
    h1 {
      font-size: 3.2rem;
      color: var(--dark);
      margin-bottom: 24px;
      font-weight: 700;
      position: relative;
      display: inline-block;
    }
    
    h1::after {
      content: '';
      position: absolute;
      height: 6px;
      width: 40%;
      background: linear-gradient(90deg, var(--primary), var(--secondary));
      bottom: -10px;
      left: 30%;
      border-radius: 30px;
    }

    p {
      font-size: 1.3rem;
      color: var(--gray-dark);
      line-height: 1.7;
      margin-bottom: 35px;
    }

    .highlight {
      color: var(--primary);
      font-weight: 600;
    }

    /* Buttons */
    .btn {
      display: inline-block;
      background: linear-gradient(135deg, var(--primary), var(--primary-dark));
      color: white;
      padding: 14px 32px;
      border-radius: 12px;
      font-weight: 600;
      font-size: 1.1rem;
      text-decoration: none;
      transition: all 0.3s ease;
      margin-top: 20px;
      box-shadow: 0 6px 15px rgba(106, 79, 235, 0.25);
      position: relative;
      overflow: hidden;
      z-index: 1;
    }
    
    .btn::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.2), transparent);
      transition: all 0.6s ease;
      z-index: -1;
    }

    .btn:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 20px rgba(106, 79, 235, 0.35);
    }
    
    .btn:hover::before {
      left: 100%;
    }

    /* Features Section */
    .features-section {
      margin-top: 60px;
      position: relative;
      z-index: 2;
      padding-top: 30px;
      border-top: 1px solid rgba(106, 79, 235, 0.1);
    }

    .feature-heading {
      font-size: 1.8rem;
      color: var(--dark);
      margin-bottom: 30px;
      position: relative;
      display: inline-block;
    }

    .feature-heading::after {
      content: '';
      position: absolute;
      height: 4px;
      width: 60%;
      background: linear-gradient(90deg, var(--accent), var(--primary-light));
      bottom: -8px;
      left: 20%;
      border-radius: 30px;
    }

    .features-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 30px;
      margin-top: 40px;
    }

    .feature-card {
      background-color: #fcfcff;
      border-radius: 12px;
      padding: 30px 20px;
      transition: all 0.3s ease;
      box-shadow: 0 4px 15px rgba(106, 79, 235, 0.05);
    }

    .feature-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 8px 25px rgba(106, 79, 235, 0.1);
    }

    .feature-icon {
      font-size: 2.5rem;
      margin-bottom: 20px;
      color: var(--primary);
      background: var(--primary-light);
      width: 70px;
      height: 70px;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
      margin: 0 auto 20px;
    }

    .feature-title {
      font-size: 1.3rem;
      font-weight: 600;
      margin-bottom: 12px;
      color: var(--dark);
    }

    .feature-desc {
      font-size: 0.95rem;
      color: var(--gray-dark);
      line-height: 1.6;
    }

    /* Responsive Design */
    @media (max-width: 900px) {
      .features-grid {
        grid-template-columns: repeat(2, 1fr);
      }
      
      .icon-left, .icon-right {
        display: none;
      }
    }

    @media (max-width: 768px) {
      .navbar-container {
        flex-direction: column;
      }
      
      .navbar-brand {
        margin-bottom: 12px;
      }
      
      .navbar-links {
        width: 100%;
        justify-content: space-around;
      }
      
      .navbar a {
        padding: 10px 14px;
        font-size: 0.9rem;
      }
      
      main {
        padding: 40px 25px;
      }
      
      h1 {
        font-size: 2.5rem;
      }
      
      p {
        font-size: 1.2rem;
      }
    }

    @media (max-width: 576px) {
      .features-grid {
        grid-template-columns: 1fr;
      }
      
      .navbar-links {
        flex-wrap: wrap;
      }
      
      .navbar a {
        margin: 4px;
      }
      
      .navbar a i {
        margin-right: 6px;
        font-size: 1rem;
      }
      
      h1 {
        font-size: 2.2rem;
      }
      
      main {
        padding: 30px 20px;
      }
      
      .btn {
        padding: 12px 28px;
      }
    }

    /* Animations */
    @keyframes fadeIn {
      from {
        opacity: 0;
        transform: translateY(30px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
  </style>
</head>

<body>
  <!-- Decorative Icons -->
  <i class="fas fa-tshirt decorative-icon icon-left"></i>
  <i class="fas fa-shopping-bag decorative-icon icon-right"></i>

  <div class="navbar">
    <div class="navbar-container">
      <a href="/home" class="navbar-brand">Dressmify</a>
      <div class="navbar-links">
        <a href="/home" class="active"><i class="fas fa-home"></i> <span>Home</span></a>
        <a href="/noleggio"><i class="fas fa-tshirt"></i> <span>Noleggio</span></a>
        <a href="/static/index.html"><i class="fas fa-comments"></i> <span>Chat</span></a>
        <a href="/logout"><i class="fas fa-sign-out-alt"></i> <span>Logout</span></a>
      </div>
    </div>
  </div>

  <main>
    <h1>Welcome Back</h1>
    <p>{{message}}</p>
    <a href="/noleggio" class="btn">Esplora i Servizi</a>

    <div class="features-section">
      <h2 class="feature-heading">Dressmify: La moda a portata di click</h2>
      <p>Dressmify è la piattaforma innovativa che ti consente di noleggiare capi d'abbigliamento di alta qualità per ogni occasione, risparmiando e rispettando l'ambiente.</p>
      
      <div class="features-grid">
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-tshirt"></i>
          </div>
          <h3 class="feature-title">Noleggio Smart</h3>
          <p class="feature-desc">Accedi a un guardaroba infinito di capi firmati, pagando solo per il tempo che li utilizzi.</p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-leaf"></i>
          </div>
          <h3 class="feature-title">Sostenibilità</h3>
          <p class="feature-desc">Riduci il tuo impatto ambientale scegliendo il noleggio invece dell'acquisto di nuovi capi.</p>
        </div>
        
        <div class="feature-card">
          <div class="feature-icon">
            <i class="fas fa-star"></i>
          </div>
          <h3 class="feature-title">Qualità Premium</h3>
          <p class="feature-desc">Solo capi selezionati dei migliori brand, controllati e igienizzati dopo ogni utilizzo.</p>
        </div>
      </div>
    </div>
  </main>
</body>

</html>